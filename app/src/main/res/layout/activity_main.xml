<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    tools:context=".MainActivity" >


    <ImageView
        android:id="@+id/imageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/youtube"
        android:layout_marginBottom="20dp"
        tools:ignore="ContentDescription" />


    <TextView
        android:id="@+id/tvProductBy"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/app_product_by"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:ignore="MissingConstraints"
        android:textSize="14sp"
        android:textColor="@color/colorPrimary"
        tools:layout_editor_absoluteY="601dp"
        app:layout_constraintTop_toBottomOf="@id/imageView"
        android:layout_above="@id/imageView"/>

    <TextView
        android:id="@+id/tvCopyRight"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/app_github"
        android:textColor="@color/colorPrimary"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:ignore="MissingConstraints"
        tools:layout_editor_absoluteY="601dp"
        app:layout_constraintTop_toBottomOf="@id/tvProductBy"
        android:layout_above="@id/tvProductBy"/>

    <ProgressBar
        android:id="@+id/splash_screen_progress_bar"
        android:theme="@style/AppTheme"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="422dp"
        android:layout_height="15dp"
        android:layout_above="@id/tvProductBy"
        android:maxHeight="10dip"
        android:minHeight="10dip"
        android:layout_marginBottom="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.545"
        app:layout_constraintStart_toStartOf="parent"
        tools:ignore="MissingConstraints" />


</androidx.constraintlayout.widget.ConstraintLayout>